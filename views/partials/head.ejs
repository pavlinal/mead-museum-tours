<!-- views/partials/head.ejs -->
<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title><%= title %></title>

    <!-- Bootstrap Core CSS -->
    <link href="css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom CSS -->
    <link href="css/mead-museum-main.css" rel="stylesheet">

    <!-- Custom Fonts -->

    <!-- Google font temporary stand in for Trajan Pro -->
    <link href='http://fonts.googleapis.com/css?family=Cinzel' rel='stylesheet' type='text/css'>
    <!-- Google font temporary stand in for Myriad Pro -->
    <link href='http://fonts.googleapis.com/css?family=PT+Sans' rel='stylesheet' type='text/css'>
    <!-- Google font for large blocks of text -->
    <link href='http://fonts.googleapis.com/css?family=Merriweather' rel='stylesheet' type='text/css'>


    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
        <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->

    <!-- jQuery -->
    <script src="js/jquery.js"></script>

    <!-- Bootstrap Core JavaScript -->
    <script src="js/bootstrap.min.js"></script>

    <!-- Javascript Functions -->
    <script type="text/javascript"> 
    $(document).ready(function(){
        console.log("ready");

        var id;
        $("#passID").click(function(){
          id=$(this).attr("data-id");
	  console.log("The ID is: " + id);
          $.post("/tourdetail",{id: id})
        });

       /*if(location.hash != ''){
            changePageHash();
        }

        $('a[href^="#"]').click(function(e){ 
            e.preventDefault();
            location.hash = $(this).attr('href');
        });*/

    });

    function onClickExpand(){
        $('.extratext').slideToggle('fast');
         return false;
    }

    function onClickAudio(){
        $('.audio').slideToggle('fast');
         return false;
    }
    function onClickVideo(){
        $('.video').slideToggle('fast');
         return false;
    }
    function onClickMap(){
        $('.map').slideToggle('fast');
         return false;
    }
    function onClickExpandTour(){
        $('.tourdescription').slideToggle('fast');
         return false;
    }

    function changePageHash(){
        if(location.hash != ''){
            var url = location.hash.substring(1);
            var startQuery = url.indexOf("?");
            if(startQuery > -1){
                url = url.substring(0, startQuery);
            }
            url = url + ".html";
        }

        console.log("Url: "+ url);

        $.ajax({ 
            type: 'GET',
            url: url,
            success: function(data) {
                $('#content').fadeOut(300, function() {
                    $('#content').html(data).delay(200).fadeIn(300);
                });
            } 
        }); 
    }

    </script>
</head>

<body onhashChange="changePageHash()">
